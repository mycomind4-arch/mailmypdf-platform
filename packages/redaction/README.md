# @mailmypdf/redaction

Reusable sensitive-document detection primitives promoted from Redact Desk. Provides deterministic fallback detection, Luhn validation for card-like candidates, masked previews, confidence-gated decisions, and page/geometry grouping. Vendor OCR/NER engines remain optional adapters rather than hard dependencies.
